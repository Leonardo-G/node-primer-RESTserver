import { ValidateIdMongoPipe } from './validate-id-mongo.pipe';

describe('ValidateIdMongoPipe', () => {
  it('should be defined', () => {
    expect(new ValidateIdMongoPipe()).toBeDefined();
  });
});
